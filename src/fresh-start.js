// Kept as a compatibility entry point for cached app shells.
// Startup must preserve existing boards, preferences and journal history.
// Destructive reset is available only through explicit in-app user actions.
export const ready = Promise.resolve();
