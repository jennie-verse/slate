# slate — 검토 보고서

최신 검토일: 2026-08-13 · 빌드: `2026.08.13-stage2.2`
이 문서는 1단계와 2단계 검토를 함께 담습니다. 1~5장이 1단계, **7장이 2단계**, **8장이 배포 후 3차 검토**, **9장이 4차 검토(상호작용)**입니다.

---

# 1단계 (빌드 `2026.08.13-stage1`)

검토일: 2026-08-13 · 빌드: `2026.08.13-stage1`
검토 환경: **Chromium(헤드리스) 실제 브라우저 구동** — iPhone 세로/가로, iPad 세로/가로 뷰포트 + 글자 크기 6·12·17px 조합 12가지, 그리고 `node --test`

이 문서는 세 가지를 구분해 적습니다.

| 구분 | 뜻 |
|---|---|
| **통과** | 이번 검토에서 실제로 확인함 |
| **고친 것** | 검토 중 발견해 수정함 |
| **Pending** | **시뮬레이터로 판정할 수 없음. Jennie가 실기기에서 확인해야 함** |

---

## 0. 요약

- 자동 테스트 **67개 전부 통과**
- **콘솔 오류 0건 · 실패한 네트워크 요청 0건** (12가지 화면 조합 전부)
- 외부로 나가는 요청 **0건** (CSP `connect-src 'self'`, 정적 검사로도 확인)
- **오프라인 동작 실제 확인** — Service Worker 설치 후 네트워크를 끊고 새로고침해도 앱이 정상 기동 (1-5)
- 검토 중 **8건을 발견해 고쳤습니다** (2장). 그중 2건은 **재검토(2차 패스)에서 나왔고, 둘 다 조용히 동작하지 않던 항목**입니다

---

## 1. 통과 항목

### 1-1. 기능

| 항목 | 결과 |
|---|---|
| 도구 10가지 그리기 | 통과 — 사각형·마름모·타원·화살표·선·자유곡선·텍스트 모두 렌더 확인 |
| 선택 → 이동 → 크기 조절 → 회전 → 삭제 | 통과 |
| 다중 선택 (드래그 범위, Shift 추가) | 통과 |
| 실행 취소 / 다시 실행 | 통과 — 8회 Undo 후 캔버스 완전히 비고, 8회 Redo로 복원됨 |
| 새로고침 후 데이터·스크롤·줌 복원 | 통과 — 리로드 후 보드 내용이 그대로 재현됨 |
| 보드 목록 (개수·용량·시각 표시) | 통과 — `2 items · 1 KB · 04:52 PM` |
| 캔버스 배경색 변경 | 통과 |
| 다크 모드 | 통과 (아래 1-4) |
| 지우개 드래그 삭제 | 통과 — 지워질 요소가 흐려지고, 한 번의 Undo로 전부 복구 |

### 1-2. 파일 형식과 왕복

| 항목 | 결과 |
|---|---|
| `.excalidraw` 내보내기 | 통과 — `{type:"excalidraw", version:2, source, elements, appState, files}` |
| `.excalidraw` 가져오기 | 통과 — 2요소 파일을 가져와 화면에 반영 |
| **미지원 필드 보존** | 통과 — `customData`·`someFutureField`가 2회 왕복 후에도 살아 있음 |
| **미지원 요소 타입** | 통과 — 등록표에 없는 타입은 회색 자리표시자로 그려지고 속성은 보존 |
| **글꼴 코드 보존** | 통과 — `fontFamily: 5`(Excalifont)가 5로 남고, 그리기만 Virgil로 대체됨 |
| **이미지 `files` 맵 보존** | 통과 — 이미지를 그리지 못해도 맵이 통째로 살아남음 |
| PNG 내보내기 | 통과 — 30~38 KB, 빈 이미지 아님 |
| SVG 내보내기 | 통과 — 84 KB, 글꼴 임베드 동작 |
| **SVG XML 이스케이프** | 통과 — `<script>alert(1)</script>`를 텍스트로 넣고 내보낸 결과 `&lt;script&gt;`로 변환됨 |
| 한글 텍스트·파일명 | 통과 — `slate-회의-노트-2026-08-13.png` 형태로 그대로 유지 |
| 전체 백업 / 복원 | 통과 — 봉투에 `schemaVersion`·`exportedAt` 포함, `:prev` 슬롯은 백업에서 제외 |

### 1-3. 화면과 접근성

12가지 조합(4방향 × 글자 3단계) 전부에서 측정한 값입니다.

| 측정 항목 | 결과 |
|---|---|
| 도구 버튼 최소 크기 | **48 × 48px** — 6px 단계에서도 동일 |
| 상단 바 넘침 | **0px** — 네 방향 모두 |
| 속성 패널이 화면 안에 있는지 | 전부 통과 |
| 도구 막대가 화면 안에 있는지 | 전부 통과 |
| 패널과 도구 막대 겹침 | **없음** |
| `aria-label` 없는 아이콘 버튼 | **0개** |
| 도구의 `aria-pressed` | 10개 전부 보유 (배경색 + 테두리 + aria 3중 표시) |
| Rail ↔ Sheet 전환 | iPad 가로에서 세로 레일(`flex-direction: column`), 그 외 하단 막대(`row`) — 미디어 쿼리 하나, DOM은 한 벌 |
| 캔버스 텍스트 편집기 | `font-size: 16px` + `transform: scale()` 확인 — iOS 자동 확대 방지 구조가 실제로 적용됨 |

### 1-4. 다크 모드 (디자인 문서 2-4의 핵심 항목)

**"채움이 있는 도형의 테두리가 다크 모드에서 사라지는가"** — 실제 픽셀을 읽어 확인했습니다.

| 모드 | 선 (실측 RGB) | 채움 (실측 RGB) |
|---|---|---|
| 라이트 | `74,58,64` = `#4A3A40` Ink | `247,227,168` = `#F7E3A8` Pastel Yellow |
| 다크 | `236,227,229` = `#EDE3E6` 밝은 Ink | `58,51,36` = `#3A3324` 어두운 Yellow |

**선과 채움이 짝으로 함께 대체되는 것이 확인되었습니다.** 명암비 9.97:1로 테두리가 뚜렷합니다. 선만 대체했다면 1.01:1이 되어 테두리가 사라졌을 자리입니다.

저장값은 바뀌지 않습니다 — 다크 모드에서 그린 그림을 라이트로 열면 원래 색입니다.

### 1-5. 코드·PWA

| 항목 | 결과 |
|---|---|
| 콘솔 오류 | **0건** |
| 실패한 요청 | **0건** |
| CSP | `connect-src 'self'` · `object-src 'none'` · `base-uri 'none'` |
| 외부 URL 참조 | 0건 (SVG 네임스페이스와 자체 배포 주소 제외) |
| 루트 절대 경로 | 0건 — 하위 경로 `/slate/`에서 동작 |
| `.nojekyll` | 포함 |
| Service Worker 캐시 버전 | `sw.js`의 `VERSION` = `src/version.js`의 `APP_BUILD` (테스트로 강제) |
| **Service Worker 실제 등록** | 통과 — 등록·활성화·페이지 제어까지 확인 |
| **프리캐시 적재** | shell 캐시 **40개** · font 캐시 **4개**, `virgil.woff2`와 `src/app.js` 모두 적재됨 |
| **오프라인 기동** | 통과 — 네트워크 차단 후 새로고침해도 도구 10개·레이어 3장 정상, 기동 오류 없음 |
| 프리캐시 목록의 파일 존재 | 전부 확인 (테스트로 강제) |
| manifest `orientation` | 없음 = 가로·세로 모두 지원 |
| 아이콘 180/192/512 + apple-touch-icon | 전부 존재 |
| 동봉 라이브러리 크기 | rough.esm.js **27,748 bytes** · perfect-freehand **4,532 bytes** (테스트로 고정) |
| 라이선스 파일 | rough · perfect-freehand · Lexend · Virgil + `THIRD_PARTY_NOTICES.md` |
| 전체 용량 | **약 580 KB** (폰트 90KB · 라이브러리 32KB 포함) |

### 1-6. 확장 지점 검증 (눈에 보이지 않는 항목)

나중을 위해 심은 구조라, 조용히 무너져도 화면상 아무 일도 일어나지 않습니다. 테스트로만 확인됩니다.

| 확장 지점 | 검증 |
|---|---|
| 1 — 변경은 `apply(action)` 한 곳으로 | 통과 — 알 수 없는 action은 예외를 던짐. 드래그·속성 패널·단축키가 전부 이 통로를 사용 |
| 2 — `version`/`versionNonce`/`updated` | 통과 — 12회 변경에 version 정확히 +12, nonce 매번 달라짐 |
| 2 — 병합 규칙의 결정성 | 통과 — 인자 순서를 바꿔도 같은 결과 (4단계 병합의 전제) |
| 3 — fractional index | 통과 — 직렬화 후 `index`만으로 정렬해도 화면 순서와 일치. 300회 중첩 삽입에도 붕괴 없음 |
| 4 — 요소 타입 등록표 | 통과 — 미등록 타입이 placeholder로 떨어지고 속성 보존 |
| 5 — 도구 수명주기 객체 | 통과 — 10개 도구가 같은 인터페이스, 속성 패널은 `propsSchema`만 읽음 |
| 6 — 캔버스 레이어 3장 | 통과 — static / interaction / overlay 확인 |
| 7 — `geometry.js`가 DOM 없이 동작 | 통과 — Node에서 25개 테스트 실행, `document`·`window` 미정의 상태를 테스트가 직접 단언 |
| 8 — 스키마 러너 | 통과 — 상위 버전 데이터를 **읽지 않고 거부** |

### 1-7. 성능 (Chromium 데스크톱 측정 — 참고값)

실기기 수치가 아닙니다. 5장 Pending 항목입니다.

| 요소 수 | rough 경로 최초 생성 | 캐시 적중 시 | 히트테스트 1회 | 전체 경계상자 |
|---|---|---|---|---|
| 200 | 23.3 ms | 0.40 ms | 0.72 ms | 0.4 ms |
| 500 | 29.2 ms | 1.10 ms | 0.36 ms | 0.2 ms |
| 1000 | 36.0 ms | 1.20 ms | 0.71 ms | 0.2 ms |
| 2000 | 69.4 ms | 3.80 ms | 1.04 ms | 0.5 ms |

경로 캐시가 최초 생성 대비 **약 20~60배** 빠릅니다. 이 캐시가 없으면 요소를 하나 옮길 때마다 전체가 다시 계산됩니다.

---

## 2. 검토 중 발견해 고친 것

| # | 문제 | 어떻게 드러났나 | 조치 |
|---|---|---|---|
| 1 | **Redo가 되돌리기를 한 번 더 실행함** | `tests/scene.test.mjs`의 undo/redo 테스트 2건 실패 | `history.js`가 되돌리기 결과의 역연산을 redo로 기록하도록 수정. 기록된 정방향 action을 재실행하면 낡은 값이 재생됨 |
| 2 | **내보내기 대화상자가 열리지 않고 조용히 멈춤** | 브라우저 검토에서 메뉴를 눌러도 아무 일도 안 일어남 | 캔버스 상한 측정이 16384²(268M px) 후보까지 시도하다 멈춘 것. 근거가 있는 두 후보(4096²·8192²)로 제한하고 실패 시 작은 쪽으로 안전 하강 |
| 3 | **메뉴 동작이 실패해도 아무 표시가 없음** | 위 2번을 찾는 데 시간이 걸린 원인 | 메뉴 핸들러를 `try/catch`로 감싸 실패를 토스트로 표시 |
| 4 | **iPhone 세로에서 상단 바가 화면 밖으로 나감** | 390px 스크린샷에서 보드 이름이 `Ur`로 잘리고 확대 버튼이 잘림 | 좁은 화면에서는 확대·축소 버튼과 되돌아가기를 메뉴로 옮기고, 플렉스 축소를 허용 |
| 5 | **시작 시 선택 도구가 활성 표시되지 않음** | 첫 스크린샷에서 어떤 도구도 눌린 상태가 아님 | `start()`에서 `selectTool("selection")` 호출 |
| 6 | **`viewportBounds`가 `-0`을 반환** | geometry 테스트가 deep-equal에서 실패 | `+ 0`으로 정규화. 저장 데이터에 `-0`이 새는 것을 방지 |

### 2차 패스에서 나온 2건 (2026-08-13, 재검토)

1차 검토를 마친 뒤 "내가 확인했다고 적었지만 실제로 실행해 보지는 않은 것"을 다시 훑어 찾은 항목입니다. **둘 다 화면상 아무 이상이 없어서 눈으로는 발견되지 않습니다.**

| # | 문제 | 어떻게 드러났나 | 조치 |
|---|---|---|---|
| 7 | **Service Worker가 아예 등록되지 않음 → 오프라인 동작 전무** | 브라우저에서 `navigator.serviceWorker.getRegistrations()`를 조회하니 **0개**. 캐시도 비어 있었음 | `registerServiceWorker()`가 `load` 이벤트 리스너만 등록했는데, `start()`가 IndexedDB를 `await`하는 사이 `load`는 **이미 끝나 있어** 리스너가 영원히 실행되지 않았음. `document.readyState === "complete"`면 즉시 등록하도록 수정 |
| 8 | **불투명도 슬라이더의 실행 취소가 동작하지 않음** | 슬라이더를 100% → 40%로 옮기고 Undo → 40%에 그대로 머무름 | 슬라이더가 `runSilent`로 실시간 미리보기를 하므로, 뒤이은 `history.run()`이 "이전 값"으로 **이미 바뀐 값**을 붙잡고 있었음. 드래그 경로와 같은 방식(`history.record(before, after)`)으로 수정 |

**7번은 이 앱을 만든 첫 번째 이유(계획서 1장)를 통째로 무효화하는 결함이었습니다.** 콘솔에는 오류가 없고 화면도 멀쩡해서, 실제로 네트워크를 끊고 새로고침해 보기 전까지는 드러나지 않습니다.

두 건 모두 회귀 테스트를 추가했습니다 — Service Worker 등록이 `readyState`를 확인하는지, 그리고 `runSilent`를 쓰는 파일이 `history.record`도 함께 쓰는지 정적 검사로 강제합니다.

---

## 3. 계획서와 다르게 한 것

| 항목 | 계획 | 실제 | 이유 |
|---|---|---|---|
| 캔버스 상한 측정 후보 | 4096² / 8192² | 그대로 | 초안에서 16384²를 추가했다가 위 2번 문제로 계획서대로 되돌림 |
| 자유 곡선 굵기 | "1/2 배율" | **`strokeWidth × 4.25`** | 원본에 `FREEDRAW_STROKE_WIDTH` 상수가 없음. `shape.ts`가 perfect-freehand에 넘기는 실제 값 |
| 손글씨 `font-family` 이름 | `Virgil` | **`Virgil 3 YOFF`** | 폰트 파일 내부 이름이 그러함. `Virgil`로 선언하면 로드되지 않음 |

---

## 4. 이번 범위에서 제외한 것 (계획대로)

2단계 항목이라 만들지 않았습니다 — 화살표 바인딩 · 도형 안 텍스트 · 그룹 · 잠금 · 정렬·분배 · 격자 스냅 · 복사·붙여넣기 · 이미지 삽입 · 캔버스 안 검색 · 링크 · 롱프레스 메뉴 · 도형 라이브러리 · 윤곽형 화살촉.

값은 1단계부터 **보존**합니다 — `startBinding`/`endBinding`/`boundElements`/`files`가 그것입니다.

---

## 5. 실기기 확인 항목 — 완료 (2026-08-13)

Jennie가 실기기에서 아래 항목을 확인했고, 배포를 막을 문제는 보고되지 않았습니다.
개별 측정값은 기록하지 않았습니다 — 아래 성능 수치 항목만 따로 남습니다.

| # | 항목 | 판정 방법 |
|---|---|---|
| 1 | **Apple Pencil 선 품질** | 같은 iPad의 Safari로 excalidraw.com을 열고, **같은 속도로 그은 선을 나란히 비교**합니다. slate 쪽 각짐이 눈에 띄게 크면 실패입니다. "부드러운가"만으로는 판정할 수 없습니다 |
| 2 | **필압·기울기 반영** | 세게/약하게 그었을 때 굵기가 달라지는지 |
| 3 | **손바닥 무시** | 실제 필기 자세로 손을 얹고 그렸을 때 손바닥 선이 안 그어지는지 |
| 4 | **줌 50% 이하에서 글자 편집 시 자동 확대** | 캔버스를 50%로 축소하고 M 크기 텍스트를 편집 → 화면이 확 커지면 실패 |
| 5 | **키보드가 열린 상태의 버튼 가림** | 텍스트 편집 중 하단 도구 막대와 시트가 키보드에 가려지지 않는지 |
| 6 | **홈 화면 앱과 Safari의 저장소 분리** | Safari에서 보드를 만들고 홈 화면 앱을 열어 **보이지 않는 것이 정상**임을 확인. 안내 문서 문구와 맞는지도 함께 |
| 7 | **요소 200/500/1000/2000개에서의 조작 지연** | 실기기 수치를 재서 이 문서 1-7에 추가 |
| 8 | **대형 보드 PNG 내보내기** | 상한에 걸렸을 때 배율이 자동으로 낮아지고 그 사실이 화면에 표시되는지, 빈 이미지가 나오지 않는지 |

> **7번(요소 200/500/1000/2000개 조작 지연)의 실측 수치는 아직 기재되지 않았습니다.**
> 이 수치는 2단계가 아니라 **3단계 진입 조건**이므로 지금 막고 있지 않습니다
> (확장 계획 6장). 2단계 사용 중에 보드가 커지면 그때 재는 편이 자연스럽습니다.

추가로 가벼운 확인:

- 한글 IME 조합 중 Enter가 텍스트 요소를 확정해 버리지 않는지
- 두 손가락 이동·핀치 줌이 Safari 기본 확대와 충돌하지 않는지
- 한글이 든 파일명이 iOS 파일(Files) 앱에서 정상 처리되는지
- 홈 화면 추가 후 장기 미사용 시 데이터가 유지되는지
- Retina 기기에서 배율 2로 내보낸 PNG의 실제 픽셀 크기가 의도한 값인지 (`devicePixelRatio` 중복 곱 확인)

---

## 6. 1단계 마무리

1. ~~GitHub Pages 배포~~ **완료 2026-08-13** — `https://jennie-verse.github.io/slate/`
2. ~~배포 주소 확인 → `Published/slate/` 이동~~ **완료**
3. ~~실기기 확인 8건~~ **완료 2026-08-13** (5장)
4. 2단계 착수 — **진입 조건의 "2주 이상 실제 사용"을 채우지 않고 착수했습니다.** 판단 근거는 7-0장.

---

# 2단계 (빌드 `2026.08.13-stage2`)

검토일: 2026-08-13
검토 환경: **Chromium(헤드리스) 실제 브라우저 구동** — iPhone 세로/가로, iPad 세로/가로 × 글자 6·12·17px 조합 12가지, 포인터로 직접 그리는 종단 시나리오, 그리고 `node --test`

## 7-0. 진입 조건에 대한 기록

확장 계획 6장의 2단계 진입 조건은 세 가지였고, 그중 **"2주 이상 실제 사용"은 충족하지 않은 채 착수했습니다.** 1단계 배포 당일(2026-08-13)에 Jennie가 전체 착수를 선택했습니다.

조건을 넣은 이유는 "안 써 보고 만든 기능은 대체로 안 쓰인다"였고, 그 위험은 **여전히 남아 있습니다.** 2단계에서 만든 것 중 실제로 안 쓰이는 기능이 나올 수 있습니다. 3단계 진입 조건의 "실제로 아쉬웠던 기능이 3개 이상"은 이번 판단과 무관하게 그대로 지킵니다.

## 7-1. 요약

- 자동 테스트 **109개 전부 통과** (1단계 67 + 2단계 42)
- **콘솔 오류 0건 · 실패한 네트워크 요청 0건** — 12가지 화면 조합, 그리고 종단 시나리오 전체
- 외부로 나가는 요청 **0건** (정적 검사 + 실행 중 관찰)
- **오프라인 동작 재확인** — 네트워크를 끊고 새로고침해도 도구 11개·레이어 3장 정상, 보드도 그대로 열림
- 검토 중 **6건을 발견해 고쳤습니다** (7-4장). 전부 화면이 멀쩡해서 눈으로는 발견되지 않는 종류입니다

## 7-2. 만든 것

계획서 14장 8번의 순서 그대로입니다.

| # | 항목 | 상태 |
|---|---|---|
| 1 | 화살표를 도형에 붙이기 (바인딩) | 완료 |
| 2 | 도형 안 텍스트 (컨테이너 바인딩, 여백 5px) | 완료 |
| 3 | 그룹 / 그룹 해제 · 잠금 | 완료 |
| 4 | 정렬 · 균등 분배 · 좌우상하 뒤집기 | 완료 |
| 5 | 격자 표시와 스냅 · 객체 스냅 보조선 | 완료 |
| 6 | 복사 / 붙여넣기 · 스타일 복사 · 클립보드 PNG/SVG | 완료 |
| 7 | 이미지 삽입 | 완료 |
| 8 | 캔버스 안 텍스트 검색 · 링크 | 완료 |
| 9 | 롱프레스 컨텍스트 메뉴 | 완료 |
| 10 | 도형 라이브러리 (`.excalidrawlib`) | 완료 |
| 그 밖 | 윤곽형 화살촉 3종 · 전체 맞춤/선택 영역 맞춤 · 단축키 전체 | 완료 |

새 모듈 10개(`binding` `containers` `arrange` `snapping` `clipboard` `images` `library` `search` `contextmenu` `tools/image`)는 전부 순수 함수 계층과 DOM 계층을 나눠 두었고, 앞의 8개는 브라우저 없이 Node에서 테스트됩니다.

## 7-3. 통과 항목

### 바인딩

| 항목 | 결과 |
|---|---|
| 도형 안에서 시작해 도형 안에서 놓으면 양쪽에 붙음 | 통과 — 포인터로 실제로 그려서 확인 |
| 양쪽 도형의 `boundElements`에 화살표가 기록됨 | 통과 |
| 도형을 **옮기면** 화살표가 따라옴 | 통과 (실제 드래그) |
| 도형의 **크기를 바꿔도** 따라옴 | 통과 |
| 화살표 끝이 도형 **바깥**에서 멈춤 | 통과 — 안쪽으로 파고들지 않음 |
| 양쪽이 다 붙은 화살표가 두 도형을 따라감 | 통과 |
| 붙은 도형을 지워도 화살표가 예외를 던지지 않음 | 통과 |
| 선(line)은 붙지 않음 | 통과 |
| **드래그 한 번 = 실행 취소 한 번** (화살표 포함) | 통과 — 실기 시나리오에서 확인 |
| **정렬 한 번 = 실행 취소 한 번** (화살표 포함) | 통과 (7-4장 4번) |
| **라벨 입력 한 번 = 실행 취소 한 번** | 통과 · 다시 실행도 한 번 (7-4장 5번) |
| **방향키 이동에 화살표가 따라옴** | 통과 (7-4장 6번) |
| 화살표 생성 + 바인딩 + `boundElements` 기록이 한 단계로 취소됨 | 통과 |

### 도형 안 텍스트

| 항목 | 결과 |
|---|---|
| 두 번 두드려 라벨 입력 | 통과 (채움 없는 도형도 안쪽에서 잡힘 — 7-4장 2번) |
| 한글 입력·보존 | 통과 — `회의 노트 hello`가 `originalText`에 그대로 |
| 자동 줄바꿈 | 통과 — 영문은 단어, 한글은 글자 단위 |
| 줄바꿈이 글자를 잃지 않음 | 통과 — 이어 붙이면 원문과 일치 |
| `originalText`가 줄바꿈된 사본으로 덮이지 않음 | 통과 |
| 글이 넘치면 도형이 세로로 늘어남 | 통과 |
| 들어가는 길이면 도형 크기를 건드리지 않음 | 통과 |
| 타원·마름모는 라벨 폭이 더 좁음 | 통과 |
| 도형을 지우면 라벨도 함께 | 통과 |
| **빈 라벨은 아무것도 남기지 않음** | 통과 — 요소도 실행 취소 단계도 생기지 않음 |

### 그 밖

| 항목 | 결과 |
|---|---|
| 정렬 6방향 | 통과 |
| 균등 분배(간격 균등) | 통과 · 3개 미만이면 거절 |
| 뒤집기 — 선택 영역 안에서 대칭, 전체 범위 유지 | 통과 |
| 선·화살표 뒤집기가 points를 반전 | 통과 |
| 그룹 중첩(append/pop) | 통과 |
| 그룹 하나를 누르면 전체 선택 | 통과 |
| 격자 스냅 · 객체 스냅 · 안내선 | 통과 · 허용 범위 밖은 안 붙음 |
| 복제·붙여넣기의 관계 재연결 | 통과 — 컨테이너·boundElements·groupIds가 사본 안에서 새로 이어짐 |
| **바깥을 가리키는 관계는 버림** | 통과 — 붙여넣은 화살표가 원본의 도형을 잡지 않음 |
| 사본의 `version`이 1로 초기화 | 통과 (4단계 병합에서 사본이 최신판으로 오인되지 않도록) |
| 이미지 삽입 → 요소·`files` 저장·실제 렌더 | 통과 (픽셀 확인) |
| 이미지가 SVG 내보내기에 임베드됨 | 통과 |
| 텍스트 검색 (한글·대소문자 무시) | 통과 |
| 검색 강조가 회전한 글자를 따라감 | 통과 (4점 좌표) |
| 윤곽형 화살촉 3종 | 통과 — 채움형과 도형은 같고 칠하는 방식만 다름 |
| 레거시 `dot` 화살촉이 여전히 그려짐 | 통과 |
| 라이브러리 저장·삽입·`.excalidrawlib` 왕복 | 통과 |
| 롱프레스/우클릭 메뉴 | 통과 — 44px 행, 화면 밖으로 나가면 뒤집힘 |
| 격자 설정이 새로고침 후에도 유지 | 통과 |

### 파일 형식 왕복 (2단계 필드)

| 항목 | 결과 |
|---|---|
| `startBinding`/`endBinding`의 `elementId`·`focus`·`gap` | 통과 — 값 그대로 |
| `containerId` · `boundElements` | 통과 |
| `circle_outline`/`triangle_outline`/`diamond_outline` | 통과 |
| `appState.gridSize` | 통과 |
| 한글 라벨 | 통과 |

### 보안

| 항목 | 결과 |
|---|---|
| 링크에 `javascript:` · `data:` · `file:` 거부 | 통과 — 입력할 때와 열 때 **두 번** 검사 |
| 사용자 텍스트가 SVG에서 마크업이 되지 않음 | 통과 — `<script>` → `&lt;script&gt;` |
| 내보낸 SVG에 `javascript:` 없음 | 통과 |
| 이미지가 `data:` URL로만 저장되고 외부를 부르지 않음 | 통과 |

### 화면과 접근성

12가지 조합(4방향 × 글자 3단계) 전부에서 측정했습니다.

| 측정 항목 | 결과 |
|---|---|
| 도구 버튼 최소 크기 | **48 × 48px** — 6px 단계에서도 동일 |
| 가로 넘침 · 세로 넘침 | **0px** — 네 방향 모두 |
| 도구 막대와 속성 패널 겹침 | **없음** |
| 세로 레일이 도구를 잘라내는지 | **0px** (7-4장 3번에서 고침) |
| `aria-label` 없는 아이콘 버튼 | **0개** |
| 컨텍스트 메뉴 행 높이 | 44px 이상 |
| 도구 개수 | 11개 (전 조합에서 전부 도달 가능) |

## 7-4. 검토 중 발견해 고친 것

| # | 문제 | 어떻게 드러났나 | 조치 |
|---|---|---|---|
| 1 | **채움 없는 도형은 안쪽을 두 번 두드려도 라벨이 안 생김** | 종단 시나리오에서 사각형 한가운데를 두 번 눌렀는데 아무 일도 안 일어남 | 선택은 지금처럼 테두리 기준으로 두고, **라벨을 넣을 때만** 안쪽을 채운 것으로 보고 판정(`containerAt`). 원본도 같은 구분을 합니다 |
| 2 | **iPhone 가로에서 도구 11개가 세로 레일에 안 들어감** | 레일 진입 조건이 `min-width: 820px`뿐이라 844×390인 iPhone 가로가 iPad 레일을 받고 있었음. 11개 × 약 50px = 550px > 390px | 레일 조건에 **`min-height: 620px`** 추가. 짧고 넓은 화면은 아래 막대를 쓰고, 속성은 오른쪽 카드로 보냄. 도구 11개가 전부 한 줄에 들어감 |
| 3 | **iPhone 세로에서 도구 막대가 잘린 것처럼 보임** | 372px 화면에 565px짜리 막대. 스크롤은 됐지만 그렇다는 표시가 없었음 | 양 끝에 마스크 페이드 + 스크롤 스냅. 잘린 것이 아니라 더 있다는 뜻으로 읽힘 |

**2번은 1단계부터 있던 문제입니다.** 도구가 10개였을 때도 390px에는 들어가지 않았고, 1단계 검토가 가로 넘침만 재고 세로를 재지 않아 놓쳤습니다. 이번에 검토 항목에 세로 넘침·레일 잘림·패널 겹침을 추가했습니다.

### 2차 패스에서 나온 3건 — "한 동작 = 한 번의 실행 취소"가 깨져 있었음

1차 검토를 마친 뒤 "만들었지만 실제로 눌러 보지는 않은 경로"를 다시 훑어 찾았습니다. **셋 다 화면상 아무 이상이 없습니다.**

| # | 문제 | 어떻게 드러났나 | 조치 |
|---|---|---|---|
| 4 | **정렬·분배·뒤집기 뒤 실행 취소를 한 번 하면 그림이 반만 되돌아감** | 바인딩된 화살표가 있는 도형 3개를 정렬하고 ⌘Z → **화살표만 제자리로 가고 도형은 정렬된 채로 남음.** 실행 취소 스택에 항목이 2개 쌓여 있었음 | `history.mergeIntoLast()` 신설. 원인이 만든 결과(화살표 재배치·라벨 재배치)를 **원인과 같은 한 단계에 접어 넣습니다** |
| 5 | **라벨을 쓴 뒤 실행 취소 한 번에 라벨이 안 지워짐** | 4번과 같은 원인. 라벨 입력 → 도형이 커짐 → 화살표 이동이 별도 단계로 기록됨 | 같은 조치. 스타일 붙여넣기와 속성 패널의 글자 크기 변경도 같은 경로였습니다 |
| 6 | **방향키로 도형을 옮기면 바인딩된 화살표가 안 따라옴** | 도형을 고르고 ↓를 눌렀을 때 화살표만 제자리에 남음. 드래그 경로에만 동기화가 있었고 키보드 경로에는 없었음 | 방향키 처리에 동기화 추가 (역시 같은 실행 취소 단계로 병합) |

**4·5번은 이 앱의 구조가 지키기로 한 규칙을 정면으로 깬 것입니다** — "요소를 바꾸는 경로는 `apply(action)` 하나뿐이고, 한 동작은 한 번의 실행 취소로 돌아간다"(확장 계획 2-1). 드래그 경로는 처음부터 지키고 있었지만(`runSilent` + `history.record`), `history.run()` 뒤에 후속 동기화를 부르는 **나머지 경로 5곳이 전부 어긋나 있었습니다.**

회귀 테스트 4개를 추가했습니다. 그중 두 개는 정적 검사입니다 — `syncBindings()` 호출은 반드시 `silent: true`(제스처 중) 또는 `merge: true`(run 직후) 중 하나여야 하고, 방향키 처리에 동기화 호출이 있어야 합니다.

## 7-5. 계획서와 다르게 한 것

| 항목 | 계획 | 실제 | 이유 |
|---|---|---|---|
| 진입 조건 | 2주 이상 실제 사용 후 착수 | 배포 당일 착수 | Jennie의 명시적 선택 (7-0장) |
| iPhone 가로 배치 | 레일 + 패널 | **막대 + 오른쪽 카드** | 도구 11개가 390px 높이에 안 들어감 (7-4장 2번) |
| 바인딩 `focus` 계산 | 계획서에 수식 없음 | 도형 중심에서 화살표 직선까지의 **수직 거리 ÷ 짧은 변의 절반** | 원본의 `determineFocusDistance`와 값이 완전히 같지는 않습니다. 관계(`elementId`)는 정확히 왕복하고, 닿는 지점만 몇 px 다를 수 있습니다 |
| 이미지 저장 | 계획서에 크기 정책 없음 | 긴 변 1600px·약 900KB를 넘으면 **줄여서 저장** | 원본 크기로 두면 사진 몇 장에 IndexedDB 할당량이 차고, 그때 잃는 것은 사진이 아니라 그림입니다 |

## 7-6. 이번 범위에서 제외한 것 (계획대로)

3단계 항목이라 만들지 않았습니다 — 프레임 · 직각(elbow) 화살표 · 선/화살표 점 편집 · 다각형 · 이미지 자르기 · 올가미 선택 · 레이저 포인터 · Tab 도형 전환 · 통계 패널 · Zen 모드 · 명령 팔레트 · PNG/SVG에 장면 데이터 심기.

`crowfoot` 화살촉과 웹 임베드는 계획서대로 **영구 제외**이며, 가져온 파일에서는 값이 보존됩니다.

## 7-7. 실기기 확인 항목 (Pending) — Jennie가 직접 확인할 것

시뮬레이터·헤드리스 브라우저로는 판정할 수 없는 항목입니다.

| # | 항목 | 판정 방법 |
|---|---|---|
| 1 | **롱프레스가 iOS 기본 동작과 부딪히지 않는지** | 캔버스를 길게 눌러 메뉴가 뜨는지, Safari의 확대경·선택 메뉴가 같이 뜨지 않는지 |
| 2 | **바인딩 판정 범위가 손가락에 적당한지** | 도형 근처에서 화살표를 놓았을 때 의도대로 붙는지, 안 붙길 바랄 때 안 붙는지 |
| 3 | **도형 안 글자 편집 중 자동 확대** | 캔버스를 50%로 줄이고 도형을 두 번 눌러 편집 → 화면이 확 커지면 실패 |
| 4 | **한글 IME 조합 중 라벨이 확정돼 버리지 않는지** | 도형 안에서 한글을 입력하다 Enter를 눌렀을 때 |
| 5 | **사진 넣기 — iOS 사진 앱 연동** | 사진/카메라 선택이 뜨는지, HEIC 사진이 들어오는지, 크기 축소 안내가 뜨는지 |
| 6 | **클립보드 복사·붙여넣기** | ⌘C/⌘V(하드웨어 키보드)와 롱프레스 메뉴 양쪽. Safari가 클립보드 읽기를 거절하면 앱 안 복사로 떨어지는지 |
| 7 | **격자·스냅이 손가락 드래그에서 방해되지 않는지** | 격자를 켜고 옮길 때 붙는 느낌이 과하지 않은지 |
| 8 | **요소 200/500/1000/2000개에서의 조작 지연** | 1단계에서 남은 항목. **3단계 진입 조건**입니다 |
| 9 | **사진이 든 보드의 용량** | 보드 목록의 용량 표시. **4단계 진입 조건**입니다 |

## 7-8. 배포 — 완료 (2026-08-13)

| 항목 | 결과 |
|---|---|
| 커밋 | `c81b899` → **`73aceff`** — "slate stage 2 — arrow binding, labels, groups, snapping, images" |
| 파일 | 신규 11 · 수정 24 · 삭제 0 |
| `npm test` (배포 직전, Mac) | 109개 전부 통과 |
| 배포 URL 응답 | `/` · `/sw.js` · `/src/binding.js` · `/src/containers.js` · `/src/tools/image.js` 전부 `200` |
| `APP_BUILD` (배포본) | `2026.08.13-stage2` |
| 브라우저 확인 | 콘솔 오류 0건 · 새 모듈 전부 200 로드 · 도구 막대에 Image 표시 |
| 실기기 확인 | Settings → About의 `Build` = `2026.08.13-stage2` (Jennie, 2026-08-13) |
| 폴더 | `Published/slate/` 그대로 유지 (이동하지 않음) |

### 배포 중 발견된 문제 1건 — 요청문의 파일 목록 누락

배포 요청문의 "이번 커밋에 들어가는 것" 목록에 **`src/history.js`가 빠져 있었습니다.**

- **원인:** 그 목록을 재검토(7-4장 4~6번) *전에* 써 두고, 재검토로 `history.js`에 `mergeIntoLast()`가 추가된 뒤에도 목록을 다시 만들지 않았습니다. 테스트 개수(105→109)만 고쳤습니다
- **결과:** 코드에는 영향이 없었습니다. Claude Code가 `git status`에서 목록에 없는 수정 파일을 발견하고 멈춰서 diff를 확인했고, 새 테스트 3개가 바로 그 메서드를 검증하는 것을 근거로 포함해 커밋한 뒤 보고했습니다 — 요청문의 "문제를 발견하면 먼저 보고" 지시대로 동작했습니다
- **재발 방지:** 파일 목록은 손으로 쓰지 않고 `git status --short` 출력에서 만듭니다 (착수 노트 6장)

## 7-9. 다음 단계

1. ~~배포~~ **완료 2026-08-13 · `73aceff`**
2. ~~배포 URL에서 새 빌드가 뜨는지 확인~~ **완료**
3. **7-7장 Pending 9건 실기기 확인** → 결과를 이 문서에 추가
4. 실사용 기록 (안 쓴 기능 · 하려다 못한 것 · 번거로웠던 것)
5. 3단계는 확장 계획 6장의 진입 조건을 확인한 뒤


---

## 8. 배포 후 3차 검토 (2026-08-13, 빌드 `2026.08.13-stage2.1`)

`2026.08.13-stage2`를 배포한 뒤, **한 번도 실제로 눌러 보지 않은 경로**를 대상으로 다시 훑었습니다. 코드 정독(부검사관 1명)과 동작 검증을 병행했습니다.

- 자동 테스트 **118개 전부 통과** (109 → +9)
- 브라우저 검사 5종 전부 통과 · 콘솔 오류 0건 · 오프라인 기동 재확인
- **결함 8건 발견, 전부 수정.** 8건 중 3건은 데이터가 조용히 망가지는 종류입니다

### 8-1. 고친 것

| # | 심각도 | 문제 | 재현 | 조치 |
|---|---|---|---|---|
| 1 | **데이터 손상** | **`.excalidraw` 가져오기가 id만 새로 만들고 관계는 안 고침** | excalidraw.com에서 라벨 붙은 도형 + 바인딩된 화살표를 내보내 가져오면 `containerId`·`boundElements`·`startBinding`이 전부 죽은 id를 가리킴. 도형을 옮겨도 라벨이 안 따라오고, 라벨을 다시 달면 매번 고아가 하나씩 늘어남 | `cloneElements()` 사용. 가져오기는 `keepSeed: true` 로 원본의 손그림 질감 유지 |
| 2 | **데이터 손상** | **순서 키가 없는 요소를 넣으면 앞뒤 순서가 id 기준으로 뒤섞임** | 순서 키가 없는 파일(구버전 Excalidraw 내보내기)의 요소 6개를 `new Scene()` 에 넣으면 `0,1,2,3,4,5` → `2,0,3,1,...` | `compareIndex`가 키 없는 요소를 `""` 로 보고 id로 비교하던 것을 수정. 키가 없으면 **배열 순서를 그대로 둡니다** |
| 3 | **데이터 손상** | **도형 라이브러리에 저장한 사진이 다른 보드에서 깨짐** | 사진을 라이브러리에 저장 → 다른 보드에서 삽입 → "Image not available" 프레임. 원본 보드에서도 `pruneFiles()` 뒤에는 같음 | 라이브러리 항목이 **사진 바이트를 함께** 보관·삽입·내보내기. 원본 형식에 없는 확장이라 다른 앱은 무시합니다 |
| 4 | 실행 취소 | **크기 조절이 라벨을 다시 접었는데 실행 취소가 그것만 안 되돌림** | 라벨 있는 도형을 좁게 줄이면 라벨이 3줄로 접힘 → ⌘Z → 도형은 원래 크기, 라벨은 3줄인 채. 그 상태가 저장·내보내기됨 | 드래그 스냅숏 항목에 `text` 추가 |
| 5 | 실행 취소 | **화살표 라벨이 실행 취소 밖에서 움직임** | 라벨 붙은 화살표의 도형을 옮기고 ⌘Z → 도형과 화살표는 돌아오는데 라벨만 빈 곳에 남음. 두 번째 ⌘Z로도 안 고쳐짐 | `bindingCompanions`가 **화살표의 라벨까지** 포함 |
| 6 | 삭제 불능 | **잠긴 요소를 붙여넣고 실행 취소하면 지워지지 않고 남음** | 잠긴 도형이 든 그림을 붙여넣기 → ⌘Z → 실행 취소는 소모됐는데 요소는 그대로. 선택도 삭제도 안 되고 Unlock all로만 복구 | 저수준 `delete`가 잠금을 무시하도록 되돌리고(이것은 `add`의 역연산이기도 하므로), **잠금 보호는 호출자(`app.deleteElements`)로** 이동 |
| 7 | 잔여물 | **글자를 쓰다 말면 실행 취소 2번 + 빈 요소가 남음** | Text 도구로 캔버스를 누르고 아무것도 안 쓰고 나가기 → ⌘Z 한 번에 폭 0짜리 보이지 않는 텍스트 요소가 복원되고 저장·내보내기됨 | 라벨과 같은 방식으로, **글자가 생기기 전에는 장면에 넣지 않습니다** |
| 8 | 표시 | **공백만 있는 줄이 사라짐** | 라벨에 `Hello` / 공백 줄 / `World` 를 쓰면 가운데 줄이 없어지고 아래가 한 줄씩 올라옴 | 모든 문단이 최소 한 줄을 만들도록 수정 |

부수로 두 가지를 함께 고쳤습니다.

- `apply("update")` 가 없는 id를 만나면 `elementIds`와 `changes`의 길이가 어긋나 실행 취소가 예외를 던지던 것 — 지금은 실제로 적용된 id만 역연산에 담습니다
- **파일 선택 창을 취소하면 `<input>`이 DOM에 영구히 쌓이던 것** — iOS는 취소 시 아무 이벤트도 주지 않습니다. 스스로 정리하는 `pickFile()` 하나로 4곳을 통일했고, **사진을 고르는 사이에 보드를 바꾸면 넣지 않고 알려 줍니다**

### 8-2. 새로 확인한 것 (전부 통과)

| 항목 | 결과 |
|---|---|
| 클립보드 왕복 — 복사한 묶음 **안쪽** 관계만 재연결 | 통과 |
| 화살표만 복사하면 바인딩이 **끊어짐** (원본 도형을 잡지 않음) | 통과 |
| 스타일 복사에 좌표·크기·id가 섞이지 않음 | 통과 |
| 사진 SHA-1 파일 id · 디코드 · 라이브러리 왕복 · 다른 보드에서 표시 | 통과 |
| `.excalidraw` **2회 연속** 왕복 후 라벨·바인딩·화살표 라벨 유지 | 통과 |
| 미지의 필드(`customData` 등) 왕복 보존 | 통과 |
| 가져온 그림이 **여전히 동작** (도형 옮기면 화살표·라벨이 따라옴) | 통과 |
| 잠금이 라벨까지 함께 잠금 | 통과 |
| 화살표 삭제가 라벨을 데려가고 양쪽 도형의 참조를 정리 · 실행 취소가 **양쪽 다** 복원 | 통과 |

### 8-3. 캐시 버전을 올린 이유

`sw.js` 자체는 안 바뀌었지만 `src/` 가 바뀌었습니다. Service Worker는 **cache-first**라 버전을 그대로 두면 이미 설치된 기기가 **옛 파일을 영원히** 씁니다. 그래서 `2026.08.13-stage2` → **`2026.08.13-stage2.1`**.

### 8-4. 이 검토는 배포되지 않았습니다

`2026.08.13-stage2.1` 은 4차 검토(9장)와 합쳐 **`2026.08.13-stage2.2`** 로 한 번에 배포합니다.


---

## 9. 4차 검토 — 상호작용 (2026-08-13, 빌드 `2026.08.13-stage2.2`)

3차 검토가 데이터와 구조를 봤다면, 이번은 **모드가 겹칠 때** 를 봤습니다. 드래그 중에 다른 것을 누르고, 두 번 두드리고, 손을 떼지 않은 채 단축키를 치는 경우입니다.

- 자동 테스트 **123개 전부 통과** (118 → +5)
- 브라우저 검사 6종 전부 통과 · 콘솔 오류 0건
- **결함 5건 발견, 전부 수정.** 그중 2건은 그린 것이 영영 돌아오지 않는 종류입니다

### 9-1. 고친 것

| # | 심각도 | 문제 | 재현 |
|---|---|---|---|
| 1 | **작업 소실** | **드래그 도중 ⌘Z를 누르면 그 요소가 영영 사라짐** | 도형을 누른 채 끌다가 손을 떼기 전에 ⌘Z → 요소가 삭제되는데 드래그는 그 시체를 계속 움직이고, 손을 떼는 순간 기록된 실행 취소가 **되살릴 수 있는 항목을 지웁니다.** 다시 실행 2번, 실행 취소 4번으로도 안 돌아옴 |
| 2 | **작업 소실** | **크기 조절 손잡이를 누르기만 해도 "다시 실행"이 사라짐** | 사각형 2개를 그리고 ⌘Z로 하나를 지운 뒤, 남은 도형의 손잡이를 **움직이지 않고 눌렀다 떼기** → 아무것도 안 바뀌었는데 실행 취소 단계가 하나 쌓이고 **되살릴 수 있던 도형이 영영 사라짐** |
| 3 | 오작동 | **링크 배지를 두 번 두드리면 링크가 두 번 열리고 라벨 편집기까지 뜸** | 배지를 더블탭 → 새 탭 2개 + 도형 안 글자 입력 시작. 이어서 뭔가 치면 라벨이 만들어짐 |
| 4 | 오작동 | 손잡이를 두 번 두드리면 빈 실행 취소 2단계 + 라벨 편집기 | 위와 같은 원인 |
| 5 | 한글 입력 크래시 | **한글 조합 중에 편집기가 닫히면 TypeError** | 조합 도중 다른 곳을 눌러 확정 → `compositionend`가 이미 사라진 `this.editing`에 쓰려다 예외 |

원인은 셋입니다.

- `history.record()` 는 **다시 실행 스택을 비웁니다.** 아무것도 안 바뀐 제스처가 그것을 부르면 안 됩니다 → 움직임이 없으면 기록하지 않도록 `move` 에만 있던 보호를 `resize`·`rotate` 로 넓혔습니다
- **손이 화면에 닿아 있는 동안의 실행 취소·삭제는 앞뒤가 맞지 않습니다** → `gestureInFlight()` 를 두고 제스처 중에는 무시합니다
- **더블탭 감지기가 방금 무슨 제스처였는지 몰랐습니다** — 선택 도구의 모든 포인터 업 뒤에 돌았습니다 → 손잡이·배지·실제 드래그는 `consumeTap()` 으로 "평범한 탭이 아니었다"고 알립니다

함께 고친 것: **라벨을 쓰는 중에 그 도형이 지워지면** 라벨이 시체를 가리킨 채 추가되어 선택도 삭제도 안 되는 유령이 되던 것.

### 9-2. 이 다섯 개의 공통점

전부 **한 동작 안에서 두 가지가 겹칠 때**만 나옵니다. 기능을 하나씩 눌러 보는 검사로는 절대 나오지 않고, 화면에도 아무 이상이 없습니다. 3차 검토까지의 모든 테스트가 통과한 상태에서 나왔습니다.

회귀 테스트 5개를 정적 검사로 추가했습니다 — 움직임 없는 제스처의 기록 금지, 제스처 중 실행 취소 금지, 더블탭의 `consumeTap` 확인, IME 핸들러의 null 가드, 죽은 컨테이너에 라벨 추가 금지.

### 9-3. 이 검토도 배포되지 않았습니다

`2026.08.13-stage2.2` 는 5차 검토(10장)와 합쳐 **`2026.08.13-stage2.3`** 으로 한 번에 배포합니다.


---

## 10. 5차 검토 — 끼어들기와 비동기 (2026-08-13, 빌드 `2026.08.13-stage2.3`)

4차 검토가 **한 손 안에서** 겹치는 경우를 봤다면, 이번은 **손이 아직 화면에 있는 동안 다른 손이 무언가를 누르는** 경우와, **기다리는 동안 상황이 바뀌는** 경우를 봤습니다. iPad에서는 두 손을 쓰는 것이 보통이므로 예외 상황이 아닙니다.

- 자동 테스트 **137개 전부 통과** (123 → +14)
- 브라우저 검사 11종 · 콘솔 오류 0건
- **결함 11건 발견, 전부 수정.** 그중 2건은 그린 것이 사라지는 종류입니다

### 10-1. 고친 것

| # | 심각도 | 문제 | 재현 |
|---|---|---|---|
| 1 | **작업 소실** | **보드를 바꾸는 순간 그리던 선이 다른 보드에 저장됨** | 보드 A에서 선을 긋는 도중 손을 떼지 않은 채 메뉴 → 보드 → B → 손을 뗌. 그 선이 **B에 저장**되고 A에는 남지 않습니다 |
| 2 | **작업 소실** | **드래그 중 보드를 바꾸면 그 위치가 영구 저장되고 되돌릴 수 없음** | 도형을 끌던 중 보드를 바꾸면 A가 **드래그 중간 위치**로 저장되고, 그것을 되돌릴 실행 취소 항목은 **B로 가버려** 눌러도 아무 일이 없습니다 |
| 3 | **작업 소실** | **사진을 넣는 도중 보드를 바꾸면 사진이 두 보드 모두에서 사라짐** | 아이폰 사진은 축소·해시에 시간이 걸립니다. 그 사이 보드를 바꾸면 사진이 **고르지 않은 보드에 들어가거나**, 저장 대기 중에 통째로 없어집니다. "사진을 추가했습니다" 안내만 뜹니다 |
| 4 | 조용한 손상 | **속성 패널의 정렬·분배·뒤집기·삭제가 드래그 중에 눌림** | 패널은 캔버스 밖이라 드래그를 끊지 않습니다. 정렬이 실행됐다가 다음 순간 드래그가 덮어써 **없던 일이 되고**, 남은 실행 취소는 **한 번도 존재한 적 없는 위치**로 되돌립니다 |
| 5 | 조용한 손상 | **화살표 키·Shift+H/V 도 같은 문제** | 삭제·실행 취소에만 보호가 있었습니다 |
| 6 | 조용한 손상 | **오른쪽 클릭 메뉴가 드래그를 끊지 않음** | 롱프레스는 끊었지만 오른쪽 클릭은 아니었습니다. 메뉴의 모든 항목이 살아 있는 드래그와 경쟁합니다 |
| 7 | 잘못된 위치 | **가져온 라이브러리 항목이 화면 밖에 삽입됨** | excalidraw.com의 `.excalidrawlib` 은 그려진 좌표를 그대로 담습니다. 그것을 0,0 기준으로 착각해 **1200px 밖**에 놓았습니다 — 눌러도 아무 일이 없는 것처럼 보입니다 |
| 8 | 원치 않은 저장 | **라이브러리 저장 창에서 "Cancel"을 눌러도 저장됨** | 빈 이름과 취소가 같은 값이라 구분이 안 됐습니다 |
| 9 | 되살아남 | **가져오기가 상대방이 지운 도형을 되살림** | `.excalidraw` 파일은 지워진 요소를 담을 수 있는데, 복사 과정이 "지워짐" 표시를 지웁니다 |
| 10 | 되살아남 | **excalidraw.com에서 붙여넣기도 같은 문제** | 시스템 클립보드로 들어오는 배열에도 지워진 요소가 들어 있습니다 |
| 11 | 왕복 손실 | **프레임 소속이 복사·가져오기에서 끊어짐** | `frameId` 만 새 id로 안 바뀌었습니다. slate는 프레임을 그리지 않아 여기서는 안 보이고, 파일을 excalidraw.com으로 되돌릴 때 자식들이 프레임에서 빠져 있습니다 |

### 10-2. 원인은 셋

- **명령이 제스처를 살피지 않았습니다.** 캔버스 밖(속성 패널·컨텍스트 메뉴)은 드래그를 끊지 않으므로, 위치를 바꾸는 모든 명령은 스스로 거절해야 합니다 → `refuseDuringGesture()` 를 12개 명령에 두고, 지우개처럼 **제스처 안에서 정당하게 호출되는** 원시 함수(`deleteElements`)는 그대로 두었습니다
- **보드를 바꾸는 길이 제스처와 저장을 챙기지 않았습니다** → `cancelGesture()` 로 먼저 끊고, 쓰다 만 라벨을 확정하고, **대기 중인 저장을 flush한 뒤** 장면을 교체합니다. 방금 삭제한 보드를 저장해 되살리는 일이 없도록 그 경로만 `flush: false` 입니다
- **`await` 뒤에 보드 id를 비교하는 것으로는 부족했습니다.** `openBoard` 는 취소·저장·로드를 마친 **뒤에야** `this.board` 를 바꾸므로, 그 사이에 끝난 사진 삽입은 여전히 옛 id와 일치했습니다 → 전환이 **시작되는 순간** 올라가는 `boardEpoch` 로 바꿨습니다

### 10-3. 검증

브라우저에서 실제 포인터로 재현한 뒤 고치고 다시 확인했습니다.

| 확인 | 결과 |
|---|---|
| 드래그 중 보드 전환 — A가 원래 위치로 저장, B에 죽은 실행 취소 없음 | 통과 |
| 그리는 중 보드 전환 — 어느 보드에도 선이 생기지 않음 | 통과 |
| 사진 준비 중 보드 전환 — 거절되고 이유가 표시됨 | 통과 |
| 패널 정렬·삭제, 화살표 키, Shift+H 를 드래그 중에 — **실행 취소 한 번**으로 원래 배치 | 통과 |
| 오른쪽 클릭 메뉴가 롱프레스와 똑같이 드래그를 끊음 | 통과 |
| 가져온 라이브러리 항목이 보이는 화면 안에 들어옴 | 통과 |
| 라이브러리 저장 창의 Cancel이 아무것도 저장하지 않음 | 통과 |
| 지워진 요소가 가져오기·붙여넣기에서 되살아나지 않음 | 통과 |
| 프레임 소속이 복사·내보내기 왕복을 견딤 | 통과 |
| 검색 창이 열린 동안 캔버스·단축키가 막힘 (기존 동작 확인) | 통과 |
| 도구 전환·핀치가 제스처를 정리함 (기존 동작 확인) | 통과 |

이미 올바르게 처리되고 있던 것도 확인했습니다 — 부분 선택 복사의 관계 정리, 복사 스냅숏이 이후 수정에 오염되지 않는 것, 라이브러리 항목을 두 번 넣어도 서로 독립인 것, 텍스트 편집기가 도구·메뉴를 누를 때 확정되는 것.

### 10-4. 회귀 테스트 14개

정적 검사 11개 — 12개 명령의 제스처 거절, 지우개가 여전히 지워지는 것, 패널·메뉴가 `deleteSelection` 을 쓰는 것, 보드 전환의 취소·확정·flush 순서, 삭제된 보드의 `flush: false`, 컨텍스트 메뉴 두 경로, `await` 뒤 재확인, epoch가 첫 `await` 앞에서 올라가는 것, Cancel과 빈 입력의 구분, 가져오기의 tombstone 필터.

동작 검사 3개 — 가져온 라이브러리 항목의 배치, `frameId` 재연결, 붙여넣기의 tombstone 필터.

### 10-5. 다음 단계

1. **재배포** — `Claude_Code_Stage2_3_Deploy_Request_2026-08-13.md` (2.1 · 2.2 · 2.3을 한 커밋으로)
2. Settings → About의 `Build` 가 `2026.08.13-stage2.3` 인지 확인
3. 7-7장 실기기 확인 9건

---

## 11. Journal 통합 검토 (2026-08-19, 빌드 `2026.08.19-journal2-stage2.3`)

2.3 배포 뒤 다른 세션에서 추가된 **Journal 기능**(Daybook에 활동을 넘기는 부분)을 검토했습니다. 커밋되지 않은 상태로 남아 있던 작업입니다.

- 자동 테스트 **144개 전부 통과** (141 → +3)
- 브라우저 검사 7종 · Journal 설정 화면 검사 1종 · **console warning/error 0건**
- **결함 3건 발견, 전부 수정**

### 11-1. 먼저, 잘 되어 있던 것

문제부터 찾았지만 이 부분은 제대로 만들어져 있었습니다.

| 확인 | 결과 |
|---|---|
| 기본값이 **꺼짐** (`slate.journalEnabled.v1`) | 통과 |
| 꺼져 있을 때 **바깥으로 나가는 요청 0건** | 통과 |
| 보내는 내용에 캔버스 요소·글자·이미지가 없음 (보드 제목과 활동만) | 통과 (기존 테스트가 검증) |
| 토큰이 기록·백업·Markdown에 섞이지 않음 | 통과 |
| 동의 문구가 설정 화면에 표시됨 | 통과 |
| 입력칸 16px · 버튼 44px | 통과 |
| 대기열 flush — `shared/v2` 가 debounce·online·visibilitychange로 처리 | 통과 |
| CSP가 `api.github.com` 하나만 추가로 허용 | 통과 |

### 11-2. 고친 것

| # | 심각도 | 문제 |
|---|---|---|
| 1 | **개인정보** | **저장한 GitHub 토큰을 지울 방법이 없었습니다.** 붙여넣기만 가능하고 삭제 버튼이 없어, 사이트 데이터를 통째로 지우지 않는 한 비공개 저장소 자격증명이 기기에 남았습니다 |
| 2 | 성능 | **journal이 꺼져 있어도 저장할 때마다 보드 전체를 JSON으로 직렬화**하고 그 문자열을 세션 내내 들고 있었습니다. 요소 2000개 자유곡선 보드 기준 **저장 1회당 32.5ms, 메모리 3.74MB** — 기본값이 꺼짐인데도 모두가 비용을 냈습니다 |
| 3 | 잘못된 기록 | **백업에서 복원한 보드를 열면, 손대지 않아도 첫 저장이 "edited"로 기록**됐습니다. `openBoard` 는 저장된 배열을, `saveNow` 는 `scene.toJSON()` 을 지문으로 썼는데, Scene은 순서 키로 정렬하고 없는 키를 채워 넣습니다. 백업은 내용을 그대로 쓰므로 둘이 어긋납니다 |

### 11-3. 고친 방법

- **1번**: `journal.removeToken()` 과 설정 화면의 "Remove token" 버튼. 확인 창을 거치고, 지우면 journal도 함께 꺼집니다. 이 키는 **같은 주소의 모든 앱이 공유**하므로 확인 문구에 그렇게 적었습니다
- **2번**: journal이 꺼져 있으면 **직렬화 자체를 건너뜁니다**(`null` 반환). 켜져 있을 때도 문자열 대신 **짧은 digest**만 남깁니다
- **3번**: `openBoard` 도 `scene.toJSON()` 을 지문으로 씁니다. 양쪽이 같은 정규형이 됩니다

### 11-4. 재현으로 확인한 값

```
 100 elements: fingerprint 1.6 ms/call,  메모리 0.19 MB
 500 elements: fingerprint 8.3 ms/call,  메모리 0.93 MB
2000 elements: fingerprint 32.5 ms/call, 메모리 3.74 MB   <- 수정 전, journal이 꺼져 있어도
```

지문 왕복 검사 (수정 전):

```
ok    이 버전이 쓴 보드는 왕복해도 같음
FAIL  순서 키가 없는 보드
FAIL  순서와 다르게 저장된 보드
FAIL  요소 하나만 키가 없는 보드
```

### 11-5. 회귀 테스트 3개

양쪽 지문이 같은 정규형을 쓰는지 · journal이 꺼져 있으면 직렬화를 건너뛰는지(그리고 그 검사가 직렬화보다 **앞에** 있는지) · 토큰 삭제 경로가 확인 창을 거치고 journal도 끄는지.

### 11-6. 남은 판단

`recordActivity` 는 실패해도 조용히 넘어갑니다(`.catch(() => {})`). 그림 작업을 막지 않으려는 의도로 보이며 상태는 설정 화면에 표시되므로 그대로 두었습니다.
